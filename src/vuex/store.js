import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

// 计算属性
const getters = {}

const state = {}

// 同步操作
const mutations = {}

// 异步操作,ajax请求
const actions = {}

export default new Vuex.Store({
  state,
  getters,
  mutations,
  actions
})
